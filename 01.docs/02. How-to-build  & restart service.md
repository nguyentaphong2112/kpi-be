# 1. SSH To server
Sử dụng công cụ ssh đến server

# 2. run câu lệnh start và build lại service
    ```
    # cd đến thư mục chứa bash script
    cd /u01/app/hcm-product/backend/bash/
    # chạy lệnh build lại service admin-service
    # (thay admin-service bằng tên service muốn restart)
    bash build-service.sh admin-service
    ```

![image](./assets/02.build-restart-service.png)
# 3. Kiểm tra kết quả build & restart service
    Sử dụng lệnh pm2 log admin-service để kiểm tra log


